def scan_func(width,length,unit):
    # will calculate number of inputs required for the stepper motors 
    # this should also be where the scope data is collected 
    if unit = 1:
        unit_symbol = 'in'
    else:
        unit_symbol = 'mm'
    print('width of the scan is ' + width + unit_symbol + ', length of the scan is '+ length + unit unit_symbol)


    # should return data of the scan