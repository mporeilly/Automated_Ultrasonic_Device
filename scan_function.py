def scan_func2(width,length,unit,voltage):
   

    print(width) # + length + unit + voltage)

    #print('width of the scan is '+ str(width.get()) + unit_symbol + ', length of the scan is '+ length +  unit_symbol)


    # should return data of the scan